@model IEnumerable<StraightPoolScore.StraightPoolGame>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>List of Games</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create") | 
        @if (StraightPoolScore.Web.MvcApplication.IsDebug) {
            <a href="http://localhost:8080/raven/studio.html" target="_blank">Raven Studio</a>
        } else {
            <a href="https://ibis.ravenhq.com/raven/studio.html#/home?api-key=ab4d08c6-3286-4778-a94e-3b3895aeb925&database=AppHarbor_979410e3-b382-4991-b710-84af96219fb0" target="_blank">Raven Studio</a>
        }
    </p>
    <table>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Limit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BallsRemaining)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumberOfInnings)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CurrentPlayerId)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Limit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BallsRemaining)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfInnings)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentPlayerId)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
